{
    "name": "aire-ai",
    "version": "1.0.0",
    "scripts": {
      "start": "node backend/index.js",
      "heroku-postbuild": "npm install --prefix client --legacy-peer-deps && npm run build --prefix client && npm install --prefix backend --legacy-peer-deps"
    },
    "engines": {
      "node": "16.x"
    }
  }
  